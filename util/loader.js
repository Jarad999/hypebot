const fs = require("fs");

module.exports = {
    async loader(client, Hyperz, config, con) {

        // THIS IS WHERE YOU CAN ADD ON TO THE READY EVENT, THIS FILE IS AUTO EXECUTED ON STARTUP

        // Technically this was cut off after the free release

    }
}
